# Copy to .env (or pass as docker -e). No domain anywhere by design.
PORT=8080
# Max WebSocket frame the server will accept (bytes). ~10MB holds an encrypted
# ~5MB file attachment (base64 + JSON + cipher overhead ≈ 1.8x).
MAX_FRAME_BYTES=10000000
